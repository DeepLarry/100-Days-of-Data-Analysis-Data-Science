"""
Day 1: Logic Building & Algorithm Efficiency
Topic: Fibonacci Sequence Generation (Iterative Approach)
Objective: Understanding Space-Time Complexity for Data Processing
"""

def generate_fibonacci(n):
    """
    Generates a Fibonacci sequence up to n terms.
    Time Complexity: O(n) - Linear time efficiency.
    Space Complexity: O(n) - Storing the sequence in a list.
    """
    if n <= 0:
        return "Please enter a positive integer."
    
    sequence = [0, 1]
    
    # Logic: Sum of the two preceding numbers
    while len(sequence) < n:
        next_val = sequence[-1] + sequence[-2]
        sequence.append(next_val)
        
    return sequence[:n]

# Driver code for testing
if __name__ == "__main__":
    terms = 10
    result = generate_fibonacci(terms)
    print(f"Fibonacci Sequence ({terms} terms): {result}")

# Technical Note: 
# This logic is foundational for understanding recursive patterns 
# often used in predictive data modeling and time-series analysis.
